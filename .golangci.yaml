version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - misspell
    - gocritic

  exclusions:
    rules:
      # errcheck is noisy in test helpers
      - path: _test\.go
        linters:
          - errcheck
