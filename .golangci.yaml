run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosimple
    - ineffassign
    - unused
    - misspell
    - gocritic

issues:
  exclude-rules:
    # errcheck is noisy in test helpers
    - path: _test\.go
      linters:
        - errcheck
